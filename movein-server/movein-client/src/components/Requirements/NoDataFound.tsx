import React from "react";
import styles from "./NoDataFound.module.css";

const NoDataFound: React.FC = () => {
  return (
    <div className={styles.divElement}>Please make sure to insert data!</div>
  );
};

export default NoDataFound;
